package org.example.Observers;

public interface NotificationAlertObserver {
    public void update();

    //now there are option user has i.e notify over mail or notify over mobile so there are two observer for observable
}
